<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.busvehicletickets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusVehicleTickets">
        <activity android:name=".Activities.ReservationToBuyActivity"></activity>
        <activity android:name=".Activities.FavoriteTicketDetailsActivity" />
        <activity android:name=".Activities.CancelTheTicketActivity" />
        <activity android:name=".Activities.CanceledTicketActivity" />
        <activity android:name=".Activities.FavoriteTicketActivity" />
        <activity android:name=".Activities.ReservedTicketActivity" />
        <activity android:name=".Deneme35Activity" />
        <activity android:name=".Activities.SeatSelectingActivity" />
        <activity android:name=".Activities.ResultActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.BoughtTicketActivity" />
        <activity android:name=".Activities.FavoriteActivity" />
        <activity android:name=".SellingActivity" />
        <activity android:name=".Activities.SearchResultActivity" />
        <activity
            android:name=".Fragments.HomeFragment"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Activities.MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.BusVehicleTickets.NoActionBar" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>